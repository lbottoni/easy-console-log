import log from './console';
import EasyMessage, {Err,Log,Info,Warn,Debug,Success,Dir} from './SHEasyMessage';

// Esporta tutto da questo file
export { Err,Log,Info,Warn,Debug,Success,EasyMessage, Dir };
